DATABASE_URL="postgresql://halleyx:halleyx123@localhost:5432/halleyx_db"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
PORT=3000
NODE_ENV=development

# ─── Email / SMTP (optional) ─────────────────────────────────────────────────
# Leave these blank to use Ethereal (fake inbox for testing)
# Fill these in to send real emails (e.g. Gmail, SendGrid, etc.)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Halleyx Workflow <noreply@halleyx.com>"

# RabbitMQ (optional — falls back to direct execution if not running)
RABBITMQ_URL=amqp://localhost:5672
